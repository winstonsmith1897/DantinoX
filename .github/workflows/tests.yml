name: Model Tests

# Run tests on every push to the main branch
on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python 3.10
      uses: actions/setup-python@v5
      with:
        python-version: "3.10"
        cache: 'pip' # Speeds up future executions

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        # Install pytest and core project libraries
        pip install pytest jax jaxlib flax optax datasets pyyaml wandb
        # If you have a requirements.txt file, uncomment the line below
        # pip install -r requirements.txt

    - name: Run Pytest
      run: |
        # Run tests in verbose mode
        python -m pytest tests/ -v