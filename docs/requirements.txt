# Documentation build dependencies — Sphinx + Read the Docs theme.
# Install with: pip install -r docs/requirements.txt

# Core Sphinx
sphinx>=7.3

# Classic Read the Docs HTML theme
sphinx-rtd-theme>=2.0

# Markdown support (MyST-Parser replaces the MkDocs markdown pipeline)
myst-parser>=3.0

# Type-hint rendering in autodoc signatures
sphinx-autodoc-typehints>=1.25

# Nice copy-button on code blocks (RTD-compatible)
sphinx-copybutton>=0.5
